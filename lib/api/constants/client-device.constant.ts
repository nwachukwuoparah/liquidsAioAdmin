/** Client platform sent on every API request. Admin app is always web. */
export const API_CLIENT_PLATFORM = "web";

/** Fallback device name when browser APIs are unavailable. */
export const CLIENT_DEVICE_NAME_FALLBACK = "Web Browser";

/** Fallback IANA timezone when runtime resolution fails. */
export const CLIENT_TIMEZONE_FALLBACK = "UTC";
