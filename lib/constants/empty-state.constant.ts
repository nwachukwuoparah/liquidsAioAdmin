/** Default illustration for reusable empty states until final assets are added. */
export const DEFAULT_EMPTY_STATE_IMAGE_SRC = "/favicon.png";

/** Shared fallback copy when a filtered list returns no rows. */
export const DEFAULT_EMPTY_STATE_DESCRIPTION =
    "Try adjusting your filters or search terms.";
